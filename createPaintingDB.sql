CREATE DATABASE IF NOT EXISTS Painting;

USE Painting;

CREATE TABLE Episodes (
    EpisodeID INT AUTO_INCREMENT PRIMARY KEY,
    Title VARCHAR(255) NOT NULL,
    AirDate DATE,
    Season INT,
    EpisodeNumber INT
);

CREATE TABLE Subjects (
    SubjectID INT AUTO_INCREMENT PRIMARY KEY,
    EpisodeID INT,
    Title NVARCHAR(255) NOT NULL,
    APPLE_FRAME BOOLEAN DEFAULT 0,
    AURORA_BOREALIS BOOLEAN DEFAULT 0,
    BARN BOOLEAN DEFAULT 0,
    BEACH BOOLEAN DEFAULT 0,
    BOAT BOOLEAN DEFAULT 0,
    BRIDGE BOOLEAN DEFAULT 0,
    BUILDING BOOLEAN DEFAULT 0,
    BUSHES BOOLEAN DEFAULT 0,
    CABIN BOOLEAN DEFAULT 0,
    CACTUS BOOLEAN DEFAULT 0,
    CIRCLE_FRAME BOOLEAN DEFAULT 0,
    CIRRUS BOOLEAN DEFAULT 0,
    CLIFF BOOLEAN DEFAULT 0,
    CLOUDS BOOLEAN DEFAULT 0,
    CONIFER BOOLEAN DEFAULT 0,
    CUMULUS BOOLEAN DEFAULT 0,
    DECIDUOUS BOOLEAN DEFAULT 0,
    DIANE_ANDRE BOOLEAN DEFAULT 0,
    DOCK BOOLEAN DEFAULT 0,
    DOUBLE_OVAL_FRAME BOOLEAN DEFAULT 0,
    FARM BOOLEAN DEFAULT 0,
    FENCE BOOLEAN DEFAULT 0,
    FIRE BOOLEAN DEFAULT 0,
    FLORIDA_FRAME BOOLEAN DEFAULT 0,
    FLOWERS BOOLEAN DEFAULT 0,
    FOG BOOLEAN DEFAULT 0,
    FRAMED BOOLEAN DEFAULT 0,
    GRASS BOOLEAN DEFAULT 0,
    GUEST BOOLEAN DEFAULT 0,
    HALF_CIRCLE_FRAME BOOLEAN DEFAULT 0,
    HALF_OVAL_FRAME BOOLEAN DEFAULT 0,
    HILLS BOOLEAN DEFAULT 0,
    LAKE BOOLEAN DEFAULT 0,
    LAKES BOOLEAN DEFAULT 0,
    LIGHTHOUSE BOOLEAN DEFAULT 0,
    MILL BOOLEAN DEFAULT 0,
    MOON BOOLEAN DEFAULT 0,
    MOUNTAIN BOOLEAN DEFAULT 0,
    MOUNTAINS BOOLEAN DEFAULT 0,
    NIGHT BOOLEAN DEFAULT 0,
    OCEAN BOOLEAN DEFAULT 0,
    OVAL_FRAME BOOLEAN DEFAULT 0,
    PALM_TREES BOOLEAN DEFAULT 0,
    PATH BOOLEAN DEFAULT 0,
    PERSON BOOLEAN DEFAULT 0,
    PORTRAIT BOOLEAN DEFAULT 0,
    RECTANGLE_3D_FRAME BOOLEAN DEFAULT 0,
    RECTANGULAR_FRAME BOOLEAN DEFAULT 0,
    RIVER BOOLEAN DEFAULT 0,
    ROCKS BOOLEAN DEFAULT 0,
    SEASHELL_FRAME BOOLEAN DEFAULT 0,
    SNOW BOOLEAN DEFAULT 0,
    SNOWY_MOUNTAIN BOOLEAN DEFAULT 0,
    SPLIT_FRAME BOOLEAN DEFAULT 0,
    STEVE_ROSS BOOLEAN DEFAULT 0,
    STRUCTURE BOOLEAN DEFAULT 0,
    SUN BOOLEAN DEFAULT 0,
    TOMB_FRAME BOOLEAN DEFAULT 0,
    TREE BOOLEAN DEFAULT 0,
    TREES BOOLEAN DEFAULT 0,
    TRIPLE_FRAME BOOLEAN DEFAULT 0,
    WATERFALL BOOLEAN DEFAULT 0,
    WAVES BOOLEAN DEFAULT 0,
    WINDMILL BOOLEAN DEFAULT 0,
    WINDOW_FRAME BOOLEAN DEFAULT 0,
    WINTER BOOLEAN DEFAULT 0,
    WOOD_FRAMED BOOLEAN DEFAULT 0,
    FOREIGN KEY (EpisodeID) REFERENCES Episodes(EpisodeID)
);

CREATE TABLE [dbo].[Colors](
	[ColorID] [smallint] IDENTITY(1,1) NOT NULL,
	[painting_index] [smallint] NOT NULL,
	[img_src] [nvarchar](100) NOT NULL,
	[painting_title] [nvarchar](50) NOT NULL,
	[season] [tinyint] NOT NULL,
	[episode] [tinyint] NOT NULL,
	[num_colors] [tinyint] NOT NULL,
	[youtube_src] [nvarchar](50) NOT NULL,
	[colors] [nvarchar](300) NOT NULL,
	[color_hex] [nvarchar](200) NOT NULL,
	[Black_Gesso] [bit] NOT NULL,
	[Bright_Red] [bit] NOT NULL,
	[Burnt_Umber] [bit] NOT NULL,
	[Cadmium_Yellow] [bit] NOT NULL,
	[Dark_Sienna] [bit] NOT NULL,
	[Indian_Red] [bit] NOT NULL,
	[Indian_Yellow] [bit] NOT NULL,
	[Liquid_Black] [bit] NOT NULL,
	[Liquid_Clear] [bit] NOT NULL,
	[Midnight_Black] [bit] NOT NULL,
	[Phthalo_Blue] [bit] NOT NULL,
	[Phthalo_Green] [bit] NOT NULL,
	[Prussian_Blue] [bit] NOT NULL,
	[Sap_Green] [bit] NOT NULL,
	[Titanium_White] [bit] NOT NULL,
	[Van_Dyke_Brown] [bit] NOT NULL,
	[Yellow_Ochre] [bit] NOT NULL,
	[Alizarin_Crimson] [bit] NOT NULL,
    FOREIGN KEY (Episode) REFERENCES Episodes(Episode)
);
